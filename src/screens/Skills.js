import { StyleSheet, Text, View } from 'react-native'
import React from 'react'
import { CustomSearchBar, CustomTable } from '../components'

const Skills = () => {
  return (
    <View>
        <CustomSearchBar />
        <CustomTable />
    </View>
  )
}

export default Skills

const styles = StyleSheet.create({})