import {
    MD3LightTheme as DefaultTheme,
    PaperProvider,
  } from 'react-native-paper';

export const theme = {
    ...DefaultTheme,
    colors: {
        primary: 'black',
        onPrimary: 'black'
    },
  };